func int add(int a, int b) {
    return a + b;
}

func int main() {
    int x = add(1, [[1, 2]]);
    return x;
}

//Description: Function "add" called with a tensor argument instead of int; should fail
