func int main() {
    tensor[int, 2, 2] A = [[1, 2], [3, 4]];

    if A {
        return 1;
    }

    return 0;
}

//Description: Condition in `if` is a tensor instead of boolean-compatible expression; should fail
